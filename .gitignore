# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets belong in local-only files and Google Secret Manager.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Heavy/generated visual assets are deployed from server-side storage rather
# than committed. Keep only the directory contract documentation in Git.
shared/public/*
!shared/public/README.md

# Built by `npm run build:connect-widget` (scripts/build-connect-widget.mjs) from
# src/connect-widget/main.ts — generated, not source. connections-routes.js
# serves it directly from public/, and returns a clear error if it's missing.
public/connect-widget.js
